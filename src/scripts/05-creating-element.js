let myBtn = document.querySelector('#testButton');

/* 
Створення
document.createElement(tagName);
*/

// const containerElem = document.querySelector('.js-container');

// const divElem = document.createElement('ul');
// divElem.classList.add('block', 'test1');
// divElem.setAttribute('alt', 'Hello');

// const usernamesElem = document.querySelector('.usernames');

// const liElems = [];

// for (let i = 0; i < 5; i++) {
//   const liElem = document.createElement('div');
//   liElem.classList.add('bold', 'blue');
//   liElems.push(liElem);
// }

// usernamesElem.append(...liElems);

/* 
    Додавання
    - element.append(el1, el2, ...) - додає один або декілька елементів після всіх дітей елемента element.
    - element.prepend(el1, el2, ...) - додає один або декілька елементів перед усіма дітьми елемента element.
    - element.after(el1, el2, ...) - додає один або декілька елементів після елемента element.
    - element.before(el1, el2, ...) - додає один або декілька елементів перед елементом element.
*/

// containerElem.prepend(divElem);
// containerElem.append(divElem);
// containerElem.after(divElem);
// containerElem.before(divElem);

/* 
Видалення
elem.remove();
*/

// const myDiv = document.querySelector('ul>div');
// myDiv.remove();

// myDiv.classList.add('bold');
// console.log(myDiv);

// document.body.append(myDiv);
